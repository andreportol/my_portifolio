SECRET_KEY=change-me
DEBUG=True

# Domínios confiáveis para CSRF (ex: https://seu-dominio.up.railway.app)
CSRF_TRUSTED_ORIGINS=https://example.up.railway.app

# Resend (preferido para envio de contato)
RESEND_API_KEY=your_resend_api_key
EMAIL_FROM=onboarding@resend.dev
CONTACT_EMAIL=destino@example.com

# SMTP opcional (não usado pelo formulário de contato com Resend)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_HOST_USER=seu_usuario
EMAIL_HOST_PASSWORD=sua_senha
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
DEFAULT_FROM_EMAIL=onboarding@resend.dev
